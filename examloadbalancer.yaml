apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: exam-pvc
  labels:
    name: exam-pvc
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
